// src/frontend/src/components/dashboard/RiskLevelsDonut.jsx
import React from 'react';

const RiskLevelsDonut = ({ riversData }) => {
    // Este será o seu gráfico de Donut. Por enquanto, é um placeholder.
    // O `riversData` já está a ser passado, o que é ótimo para quando implementarmos o gráfico de verdade.
    return (
        <div style={{ color: 'var(--text-light)', textAlign: 'center', padding: '20px' }}>
            Gráfico de Níveis de Risco (em construção)
        </div>
    );
};

export default RiskLevelsDonut;