// src/frontend/src/components/dashboard/TopRiversChart.jsx
import React from 'react';

const TopRiversChart = ({ riversData }) => {
    // Este será o seu gráfico de Barras dos Top Rios. Por enquanto, é um placeholder.
    // O `riversData` já está a ser passado.
    return (
        <div style={{ color: 'var(--text-light)', textAlign: 'center', padding: '20px' }}>
            Gráfico Top Rios por Risco (em construção)
        </div>
    );
};

export default TopRiversChart;